function timer_func(obj,event)
txt = 'test';
disp(txt)

sample_rate = get(obj,'SampleRate');
samples_tot = get(ai,'SamplesPerTrigger');

trig_time -=


duration = samples_tot ./ sample_rate;